# Merged tasks: Settings page + Notification channels

Combined task branches:
- `agent/settings-page` — Build out Settings page with org info, user management UI
- `agent/alert-notification-channels` — Add per-rule notification channels (email/webhook)
